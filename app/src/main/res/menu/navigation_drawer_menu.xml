<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group>
        <item
            android:title="@string/main_menu"
            app:showAsAction="always"
            android:icon="@drawable/ic_home_black_24dp"
            android:id="@+id/main_menu"/>
        <item
            android:title="@string/online_storage"
            app:showAsAction="always"
            android:icon="@drawable/ic_cloud_black_24dp"
            android:id="@+id/online_storage"/>
        <item
            android:title="@string/password_generator"
            app:showAsAction="always"
            android:icon="@drawable/ic_key"
            android:id="@+id/pass_generator"/>
    </group>
    <group android:id="@+id/group" android:checkableBehavior="single">
        <item
            android:title="@string/log_out"
            app:showAsAction="always"
            android:icon="@drawable/ic_log_out"
            android:id="@+id/log_out"/>
    </group>
</menu>